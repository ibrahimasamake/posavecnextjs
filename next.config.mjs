/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['www.shutterstock.com', 'odoo.consulting', 'cdn.pixabay.com/','www.afex-experts-comptables.fr','source.unsplash.com','via.placeholder.com'],
    },
};

export default nextConfig;
