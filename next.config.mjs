/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['www.shutterstock.com', 'odoo.consulting', 'www.afex-experts-comptables.fr','source.unsplash.com'],
    },
};

export default nextConfig;
